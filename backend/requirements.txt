# AttendAI — Python 3.12+ / 3.14 compatible
# No dlib, no cmake, no C++ compiler needed!
# Uses OpenCV's built-in Haar Cascade for face detection

flask==3.1.0
flask-cors==5.0.0
numpy==2.1.0
opencv-contrib-python==4.10.0.84
Pillow==10.4.0
